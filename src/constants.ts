/** CSS class added to document element during back navigation for reverse animation */
export const BACK_NAVIGATION_CLASS = 'back-navigation';

/** CSS class added to document element for Android-specific animations */
export const ANDROID_PLATFORM_CLASS = 'android';

/** CSS class for fade animation */
export const ANIMATION_FADE_CLASS = 'animation-fade';

/** CSS class for zoom animation */
export const ANIMATION_ZOOM_CLASS = 'animation-zoom';

/** CSS class for no animation (instant) */
export const ANIMATION_NONE_CLASS = 'animation-none';

/** CSS class for lift animation (explicit, not platform-based) */
export const ANIMATION_LIFT_CLASS = 'animation-lift';

/** CSS class for slide animation (explicit, not platform-based) */
export const ANIMATION_SLIDE_CLASS = 'animation-slide';
