# Use exact versions for reproducible builds
save-exact=true

# Scoped package registry
@lemoncloud:registry=https://registry.npmjs.org/

# CI auth (NPM_TOKEN env var set in GitHub Actions)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
